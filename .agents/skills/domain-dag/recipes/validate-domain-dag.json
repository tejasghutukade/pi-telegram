{
  "name": "validate-domain-dag",
  "description": "Validate Domain DAG architecture metadata for a repository root.",
  "args": ["root:path=."],
  "defaults": {
    "skill_dir": "~/.pi/agent/skills/domain-dag"
  },
  "template": "{skill_dir}/scripts/validate-domain-dag.mjs --root {root}"
}
